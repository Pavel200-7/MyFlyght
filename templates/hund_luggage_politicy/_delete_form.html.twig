<form method="post" action="{{ path('app_hund_luggage_politicy_delete', {'id': hund_luggage_politicy.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hund_luggage_politicy.id) }}">
    <button class="btn">Delete</button>
</form>
