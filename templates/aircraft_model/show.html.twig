{% extends 'base.html.twig' %}

{% block title %}AircraftModel{% endblock %}

{% block body %}
    <h1>AircraftModel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ aircraft_model.id }}</td>
            </tr>
            <tr>
                <th>Model</th>
                <td>{{ aircraft_model.model }}</td>
            </tr>
            <tr>
                <th>MaxSits</th>
                <td>{{ aircraft_model.MaxSits }}</td>
            </tr>
            <tr>
                <th>MaxWeight</th>
                <td>{{ aircraft_model.MaxWeight }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_aircraft_model_index') }}">back to list</a>

    <a href="{{ path('app_aircraft_model_edit', {'id': aircraft_model.id}) }}">edit</a>

    {{ include('aircraft_model/_delete_form.html.twig') }}
{% endblock %}
