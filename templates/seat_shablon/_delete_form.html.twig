<form method="post" action="{{ path('app_seat_shablon_delete', {'id': seat_shablon.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ seat_shablon.id) }}">
    <button class="btn">Delete</button>
</form>
