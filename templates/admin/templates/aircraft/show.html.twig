{% extends 'admin/baseAdmin.html.twig' %}

{% block title %}Aircraft{% endblock %}

{% block body %}
    <h1>Aircraft</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ aircraft.id }}</td>
            </tr>
            <tr>
                <th>ManufactureDate</th>
                <td>{{ aircraft.ManufactureDate ? aircraft.ManufactureDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>RegistrationNumber</th>
                <td>{{ aircraft.registrationNumber }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_aircraft_index') }}">back to list</a>

    <a href="{{ path('app_aircraft_edit', {'id': aircraft.id}) }}">edit</a>

    {{ include('admin/templates/aircraft/_delete_form.html.twig') }}
{% endblock %}
