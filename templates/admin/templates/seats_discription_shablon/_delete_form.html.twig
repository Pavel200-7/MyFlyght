<form method="post" id="myDeleteFormId" action="{{ path('app_seats_discription_shablon_delete', {'id': seats_discription_shablon.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ seats_discription_shablon.id) }}">
</form>
