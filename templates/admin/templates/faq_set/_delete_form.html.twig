<form method="post" id="myDeleteFormId" action="{{ path('app_faq_set_delete', {'id': faq.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ faq.id) }}">
</form>
