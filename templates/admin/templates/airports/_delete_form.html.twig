<form method="post" id="myDeleteFormId" action="{{ path('app_airports_delete', {'id': airport.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ airport.id) }}">
</form>
