{% extends 'base.html.twig' %}

{% block title %}Manufacturers{% endblock %}

{% block body %}
    <h1>Manufacturers</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ manufacturer.id }}</td>
            </tr>
            <tr>
                <th>ManufacturerName</th>
                <td>{{ manufacturer.manufacturerName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_manufacturers_index') }}">back to list</a>

    <a href="{{ path('app_manufacturers_edit', {'id': manufacturer.id}) }}">edit</a>

    {{ include('manufacturers/_delete_form.html.twig') }}
{% endblock %}
